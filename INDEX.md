# sam-3-prototype

UI mockup → design handoff spec. Published as `samout`; see [README.md](README.md)
for architecture, install and results.

Local-only (gitignored): `input/` source mockups, `assets/` pipeline output,
`models/` weights (3.5 GB), `.venv/`.
